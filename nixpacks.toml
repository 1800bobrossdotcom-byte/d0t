# Tell Nixpacks to ignore root and only use web/
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd web && npm ci || npm install"]

[phases.build]
cmds = ["cd web && npm run build"]

[start]
cmd = "cd web && npm run start"
